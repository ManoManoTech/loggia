---
extends: default

ignore: |
  build
  dev_packages
  packages
  .venv

rules:
  line-length:
    max: 140
    level: warning
  comments-indentation:
    ignore: "*"
  indentation:
    spaces: 2
    indent-sequences: consistent
